display_information:
  name: LangChain Bot
features:
  bot_user:
    display_name: LangChain Bot
    always_online: false
oauth_config:
  scopes:
    bot:
      - app_mentions:read
      - chat:write
settings:
  event_subscriptions:
    request_url: https://xxx.lambda-url.ap-northeast-1.on.aws/
    bot_events:
      - app_mention
  # interactivity:
  #   is_enabled: true
  org_deploy_enabled: false
  socket_mode_enabled: true
  token_rotation_enabled: false
